# GITHUB AUTOSAVE RULE

Every code/error/requirement change must be committed and pushed.

Required repo:
https://github.com/sagarkerhalkar/SagarSystemHealthMonitor-V10

Every future apply script must call:

powershell -ExecutionPolicy Bypass -File scripts\GIT_AUTO_COMMIT_PUSH.ps1 -Message "meaningful change note"

Do not ask user every time. Update automatically.
